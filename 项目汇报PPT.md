---
marp: true
theme: default
paginate: true
style: |
  section {
    font-size: 24px;
  }
  .columns {
    display: flex;
    gap: 40px;
  }
  .columns .col {
    flex: 1;
  }
---

# 易宿酒店预订平台

> 第五期携程前端训练营大作业
> 冯俊财 · 黄彬健

---

## 一、项目概述

<div class="columns"><div class="col">

### 项目背景

随着在线旅游市场的快速发展，酒店预订已成为用户出行的核心需求之一。
本项目以携程酒店业务为参考，设计并实现了一套酒店预订平台。

### 平台角色

| 角色 | 使用端 | 核心功能 |
|------|--------|----------|
| 用户 | 移动端 | 搜索酒店、浏览详情 |
| 商户 | PC管理端 | 录入酒店、管理房型 |
| 管理员 | PC管理端 | 审核酒店、管理上下线 |

</div><div class="col">

### 技术栈

| 层级 | 技术选型 |
|------|----------|
| 移动端 | React 18 + Taro 4 + Sass |
| PC管理端 | React 18 + Vite 5 + Ant Design 5 |
| 后端服务 | Node.js 18 + Express 4 |
| 数据库 | MySQL 8（Docker 部署） |
| 认证 | JWT + bcrypt |
| 地图服务 | 高德地图 Web API |
| 部署 | Nginx + PM2 + Docker Compose |

</div></div>

---

## 二、功能模块 — 移动端

<div class="columns"><div class="col">

### 2.1 首页模块

- **城市选择**：国内/海外双分类，60+ 城市
- **自定义日历组件**：12 个月可滚动，入住/离店日期范围选择
- **价格区间滑块**：双滑块拖拽（0~2000+），步进吸附
- **人数/房间数选择**：计数器交互
- **推荐酒店轮播**：Banner 展示
- **热门标签筛选**：后端动态获取
- **定位功能**：高德地图 API 自动获取所在城市

</div><div class="col">

### 2.2 酒店列表模块

- **多条件筛选**：城市 + 关键词 + 价格区间 + 星级 + 标签
- **多种排序**：综合、价格、评分、距离
- **关键词搜索**：支持酒店名、地址、附近地点
- **上拉无限滚动**：分页加载（每页 10 条）
- **筛选编辑面板**：「草稿-确认」模式

### 2.3 酒店详情模块

- **图片轮播**：沉浸式 Banner
- **三 Tab 展示**：概况 / 设施 / 政策
- **房型列表**：价格、面积、床型、早餐等
- **下拉刷新**：获取最新数据

</div></div>

---

## 三、功能模块 — PC管理端

<div class="columns"><div class="col">

### 3.1 登录/注册

- 统一登录入口，支持商户和管理员
- JWT Token 本地持久化
- 根据角色自动跳转对应仪表盘

### 3.2 商户仪表盘

- 表格展示商户旗下所有酒店
- 审核状态/发布状态标签化展示
- 快捷操作：新增酒店、编辑酒店

### 3.3 酒店信息录入/编辑

- 多步骤分页表单 + Steps 进度条
- Tab 切换：酒店信息 / 房型管理
- 动态增删房型，每个房型独立表单

</div><div class="col">

### 3.4 审核管理

- 待审核酒店队列表格
- 状态筛选：全部 / 待审核 / 已通过 / 已驳回
- 审核操作：通过 / 驳回（需填写原因）

### 3.5 路由守卫

- 未登录 → 重定向登录页
- 角色不匹配 → 重定向对应默认页
- Token + Role 双重验证

</div></div>

---

## 三（续）、酒店表单提交流程

<div class="columns"><div class="col">

### 整体设计

- 多步骤分页，顶部 Steps 进度条指示位置
- 最后一步统一提交，而非每步单独提交

### 数据收集方式

- **酒店信息层**：`Form.useForm()` 创建单一表单实例，向下传递给各子组件，提交时调用 `form.getFieldsValue()` 一次取回全部字段
- **房间信息层**：每个房间独立子表单，父组件用 `useRef` 维护映射表（`roomFormsRef`），提交时遍历批量取值

</div><div class="col">

### 提交前数据清洗

- 将 `undefined` 字段替换为 `null`
- 原因：MySQL 期望空字段为 `NULL`，JS 未填写项默认 `undefined`，直接提交会报错
- 最终结构：酒店对象 + 房间数组合并为完整 JSON，后端统一接收

</div></div>

---

## 四、系统架构

```
┌─────────────────┐     ┌──────────────────┐
│   移动端 (Taro)  │     │  PC管理端 (Vite)  │
│  H5 / 微信小程序  │     │  商户端 / 管理端   │
└────────┬────────┘     └────────┬─────────┘
         │  HTTPS (443)          │  HTTPS (8081)
         └───────────┬───────────┘
                     ▼
          ┌─────────────────────┐
          │   Nginx 反向代理     │
          │  (SSL + 静态资源托管) │
          └──────────┬──────────┘
                     │ /api → 127.0.0.1:5000
                     ▼
          ┌─────────────────────┐
          │  Express API Server │
          │  (PM2 + JWT 认证)   │
          └──────┬──────┬───────┘
                 │      │
        ┌────────▼─┐  ┌─▼──────────┐
        │  MySQL 8  │  │ 高德地图 API │
        │ (Docker)  │  │ (地理编码)   │
        └──────────┘  └────────────┘
```

---

## 五、数据库设计

<div class="columns"><div class="col">

### 7 张核心数据表

| 数据表 | 说明 |
|--------|------|
| `merchant` | 商户账号 |
| `admin` | 管理员 |
| `hotel` | 酒店信息 |
| `room_type` | 房型字典 |
| `hotel_room` | 酒店房间 |
| `tag` | 标签/设施 |
| `hotel_tag_relation` | 酒店-标签关联 |

</div><div class="col">

### 核心关系

```
merchant (1) ──→ (N) hotel
hotel    (1) ──→ (N) hotel_room
hotel    (M) ←──→ (N) tag
```

### 设计亮点

- **全文检索索引**：FULLTEXT + ngram，支持中文搜索
- **审核双状态**：`audit_status` + `publish_status` 控制上线
- **标签体系**：多对多关联表

</div></div>

---

## 六、技术亮点

<div class="columns"><div class="col">

### 6.1 自定义日历组件

- 完全手写，非第三方库
- 12 个月可滚动，7 列网格布局
- 入住/离店范围选择，中间日期高亮
- 凌晨特殊处理（0:00-6:00）
- `useMemo` 优化性能

### 6.2 价格区间双滑块

- 纯手写触摸交互组件
- 双滑块独立拖拽，步进吸附
- 触摸事件拦截，防止页面滚动
- `useRef` 轨道宽度实时计算

</div><div class="col">

### 6.3 高德地图集成

- **距离计算**：地理编码获取坐标，计算距离，按距离排序
- **关键词识别**：搜索时判断是地点还是酒店名，自动切换搜索方式

### 6.4 综合排序加权算法

```
综合分 = 评分/5×40 + 星级/5×30
       + (1-价格/2000)×20
       + 新店加成×10
```

| 因子 | 权重 | 说明 |
|------|------|------|
| 评分 | 40% | 用户口碑优先 |
| 星级 | 30% | 品质保障 |
| 性价比 | 20% | 价格越低分越高 |
| 新店 | 10% | 30天内加成 |

</div></div>

---

## 七、团队分工

| 成员 | 负责模块 | 占比 |
|------|----------|------|
| **冯俊财** | 移动端及相关后端、服务器部署 | 50% |
| **黄彬健** | PC端及相关后端 | 50% |

---

## 演示 & Q&A

感谢观看！
