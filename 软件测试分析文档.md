# 易宿酒店预订平台 — 软件测试分析文档

> 基于 ISTQB 测试流程与测试原则，对"易宿"酒店预订平台项目进行分析。

---

## Exercise 1.4.1: 测试步骤与执行的任务

> 回顾 ISTQB 测试流程中哪些步骤被执行了、哪些没有执行。

### 1. Planning and Control（计划与控制）— ⚠️ 部分执行

**已执行：**

- 项目有明确的需求文档（`项目.md`）和开发日志（`development_log.md`），定义了功能范围
- 使用 Git 分支策略（main / mobile / management）进行版本控制
- 使用 conventional commits（`feat:`, `fix:`, `docs:`）规范提交信息

**未执行：**

- 没有正式的**测试计划文档**（test plan）
- 没有定义测试范围、测试策略、测试时间表
- 没有明确的质量目标或测试退出标准

---

### 2. Analysis and Design（分析与设计）— ⚠️ 部分执行

**已执行：**

- 项目有数据库设计文档（`数据库设计.md`），定义了表结构和关系
- 代码分析文档（`code_analysis_log.md`）分析了系统架构
- 答辩准备文档（`答辩拷打准备_log.md`）中包含了对潜在问题的预判

**未执行：**

- 没有设计正式的**测试用例**（test cases）
- 没有识别和记录**测试条件**（test conditions）
- 没有设计测试数据（仅有 `schema.sql` 初始化脚本）

---

### 3. Implementation and Execution（实施与执行）— ❌ 基本未执行

**已执行：**

- 配置了 **ESLint**（`.eslintrc.js`）进行静态代码检查
- 配置了 **Prettier**（`.prettierrc`）进行代码格式化
- Mobile 端配置了 **Husky** git hooks 和 **commitlint** 来保证提交质量
- 开发过程中通过手动测试发现并修复了 bug（见 `problem_log.md`）

**未执行：**

- **没有任何自动化测试框架**（无 Jest、Vitest、Mocha 等）
- 没有单元测试、集成测试或端到端测试
- 没有创建 test harness 或 test suite
- 没有测试脚本或测试执行记录

---

### 4. Evaluating Test Exit Criteria and Reporting（评估退出标准与报告）— ❌ 未执行

- 没有定义测试退出标准
- 没有测试结果日志或测试报告
- 没有缺陷追踪系统（GitHub Issues 也未使用）
- 没有测试覆盖率报告

---

### 5. Test Closure Activities（测试收尾）— ❌ 未执行

- 没有测试总结报告（test summary report）
- 没有进行测试回顾（retrospective）
- 没有归档测试产物（testware）
- 有项目汇报 PPT，但不包含测试相关内容

---

### 步骤重叠与并行情况

| 现象 | 说明 |
|------|------|
| **开发与手动测试并行** | 开发者在编码时同时进行手动功能验证，"编码-测试"交替进行 |
| **静态分析与开发并行** | ESLint / Prettier 在开发过程中持续运行，相当于持续的代码质量检查 |
| **计划与实施重叠** | 需求和设计在开发过程中逐步细化，没有严格分阶段 |

### 小结

该项目作为训练营大作业，**重点放在了功能实现上，测试几乎完全依赖手动测试**。ISTQB 5 个测试流程中，只有第 1、2 步被部分执行，第 3 步仅有静态代码分析，第 4、5 步完全缺失。这在时间紧迫的学生项目中很常见，但从专业角度看，缺乏系统性的测试流程是主要的改进方向。

---

## Exercise 1.3.1: 观察到的测试原则（Test Principles Observed and Not）

> 回顾 ISTQB 七大测试原则，哪些在项目中被遵守了，哪些被违反了。

### ✅ 被遵守的原则

**原则 2: Exhaustive testing is impossible（穷尽测试是不可能的）**

- 项目隐式地遵守了这一原则 — 开发者聚焦于核心功能的手动验证（搜索、预订、审核流程），而非试图测试所有可能的输入组合
- 例：酒店搜索功能重点验证了关键词搜索、价格过滤、日期选择等核心场景

**原则 5: Pesticide paradox（杀虫剂悖论）— 部分体现**

- 从 `problem_log.md` 和开发日志可以看到，修复一个 bug 后经常发现新的问题
- 例：修复详情页预订交互后，又发现了开业时间显示和无限滚动的新问题（commit `ca9e5d5`）

**原则 7: Absence-of-errors fallacy（无错误谬误）— 被理解**

- 项目不仅关注"代码是否有 bug"，还关注用户体验（答辩准备文档中讨论了性能优化、交互细节等）

---

### ❌ 被违反的原则

**原则 1: Testing shows the presence of defects（测试证明缺陷存在）**

- **违反**：项目没有系统性的测试来主动发现缺陷。Bug 主要通过开发者的偶然发现和手动操作暴露
- 例：没有边界值测试（如价格为 0 或负数时的行为）、没有异常输入测试

**原则 3: Early testing（尽早测试）**

- **严重违反**：项目完全没有在开发早期引入测试
- 没有 TDD（测试驱动开发），没有在开发阶段编写单元测试
- 所有"测试"都是在功能开发完成后的手动验证

**原则 4: Defect clustering（缺陷聚集）**

- **未被利用**：从开发日志看，多个 bug 集中在移动端的日期处理和列表滚动模块，但项目没有对这些高风险模块进行针对性的深度测试
- 如果利用这一原则，应该对 `CalendarPicker` 组件和 `useLoadMore` hook 进行重点测试

**原则 6: Testing is context dependent（测试依赖上下文）**

- **部分违反**：项目没有根据不同模块的风险级别调整测试策略
- 后端 API（涉及数据安全、JWT 认证）理应比前端 UI 需要更严格的测试，但两者都没有自动化测试

---

### 讨论

该项目最大的测试缺陷是**完全缺乏自动化测试**。如果要改进，优先级建议：

1. **后端 API 测试**（最高优先级）— 使用 Jest + Supertest 测试认证、预订、审核等核心接口
2. **前端关键组件单元测试** — 使用 Vitest 测试 CalendarPicker、PriceRangeSlider 等复杂组件
3. **端到端测试** — 使用 Cypress / Playwright 测试完整的预订流程

这反映了学生项目在时间压力下的常见取舍：**功能完整性优先于质量保障**，这在学习阶段可以理解，但在实际工程中需要改进。
