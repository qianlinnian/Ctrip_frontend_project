# 易宿酒店预订平台 - 数据库设计文档

## 1. user（用户表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| user_id | bigint | 用户ID | 主键、自增 |
| username | varchar(50) | 用户名 | 唯一、非空 |
| password | varchar(255) | 密码（加密存储） | 非空 |
| phone | varchar(20) | 手机号 | 唯一、非空 |
| sign_up_time | datetime | 注册时间 | 默认当前时间 |

---

## 2. merchant（商户表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| merchant_id | bigint | 商户ID | 主键、自增 |
| merchant_name | varchar(100) | 商户名称 | 非空 |
| phone | varchar(20) | 联系电话 | 唯一、非空 |
| password | varchar(255) | 密码（加密存储） | 非空 |
| business_license | varchar(255) | 营业执照URL | |
| status | int | 账号状态 | 默认1（1正常/2禁用） |
| create_time | datetime | 创建时间 | 默认当前时间 |

---

## 3. admin（管理员表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| admin_id | bigint | 管理员ID | 主键、自增 |
| username | varchar(50) | 用户名 | 唯一、非空 |
| password | varchar(255) | 密码（加密存储） | 非空 |
| phone | varchar(20) | 手机号 | 唯一 |
| create_time | datetime | 创建时间 | 默认当前时间 |

---

## 4. hotel（酒店表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| hotel_id | bigint | 酒店ID | 主键、自增 |
| merchant_id | bigint | 所属商户ID | 外键、非空 |
| hotel_name | varchar(100) | 酒店名称 | 非空 |
| city | varchar(50) | 所属城市 | 非空 |
| hotel_address | varchar(255) | 详细地址 | 非空 |
| hotel_level | int | 酒店星级 | 1-5 |
| phone | varchar(20) | 联系电话 | 非空 |
| price_start | decimal(10,2) | 起始价格 | 非空 |
| room_count | int | 房间总数 | 非空 |
| description | text | 酒店简介 | |
| images | text | 酒店图片（JSON数组） | |
| audit_status | int | 审核状态 | 默认0（0草稿/1审核中/2通过/3拒绝） |
| publish_status | int | 发布状态 | 默认0（0未发布/1已发布/2已下线） |
| reject_reason | varchar(255) | 拒绝原因 | |
| offline_reason | varchar(255) | 下线原因 | |
| create_time | datetime | 创建时间 | 默认当前时间 |

**外键约束:**
- `merchant_id` → `merchant(merchant_id)`

---

## 5. room_type（全局房型字典表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| type_id | bigint | 房型ID | 主键、自增 |
| type_name | varchar(50) | 房型名称 | 唯一、非空（如：豪华大床房、商务双床房） |

---

## 6. hotel_room（酒店房型表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| room_id | bigint | 记录ID | 主键、自增 |
| hotel_id | bigint | 所属酒店ID | 外键、非空 |
| room_type_id | bigint | 房型ID | 外键、非空 |
| room_count | int | 该房型数量 | 非空 |
| price | decimal(10,2) | 价格/晚 | 非空 |
| images | text | 房型图片（JSON数组） | |

**外键约束:**
- `hotel_id` → `hotel(hotel_id)`
- `room_type_id` → `room_type(type_id)`

---

## 7. room_booking（房间预订表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| book_id | bigint | 预订ID | 主键、自增 |
| user_id | bigint | 用户ID | 外键、非空 |
| room_id | bigint | 酒店房型ID | 外键、非空 |
| start_time | datetime | 入住时间 | 非空 |
| end_time | datetime | 退房时间 | 非空 |
| total_price | decimal(10,2) | 总价 | 非空 |
| status | int | 订单状态 | 默认1（1待支付/2已支付/3已入住/4已完成/5已取消） |
| booking_time | datetime | 预订时间 | 默认当前时间 |

**外键约束:**
- `user_id` → `user(user_id)`
- `room_id` → `hotel_room(room_id)`

---

## 8. tag（标签表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| tag_id | bigint | 标签ID | 主键、自增 |
| tag_name | varchar(50) | 标签名称 | 唯一、非空 |

---

## 9. hotel_tag_relation（酒店标签关联表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | bigint | 关联ID | 主键、自增 |
| hotel_id | bigint | 酒店ID | 外键、非空 |
| tag_id | bigint | 标签ID | 外键、非空 |

**外键约束:**
- `hotel_id` → `hotel(hotel_id)`
- `tag_id` → `tag(tag_id)`

**唯一约束:**
- `(hotel_id, tag_id)` 联合唯一

---

## 数据库关系图

```
user (1) ----< (N) room_booking (N) >---- (1) hotel_room (N) >---- (1) room_type
                                               |
                                               |
merchant (1) ----< (N) hotel (1) ----< (N) hotel_room
                     |
                     |
tag (N) >---- (N) hotel_tag_relation (N) >---- (1) hotel
```

---

## 核心业务流程

### 商户注册并提交酒店
1. 商户在 `merchant` 表注册
2. 创建酒店记录到 `hotel` 表，`audit_status=1`（审核中）

### 管理员审核酒店
1. 管理员查看 `hotel` 表中 `audit_status=1` 的记录
2. 审核通过：更新 `audit_status=2`，可选择发布
3. 审核拒绝：更新 `audit_status=3`，填写 `reject_reason`

### 酒店发布与下线
1. 发布：更新 `publish_status=1`
2. 下线：更新 `publish_status=2`，填写 `offline_reason`
3. 恢复：更新 `publish_status=1`

### 商户添加房型
1. 从 `room_type` 表选择房型（如：豪华大床房）
2. 在 `hotel_room` 表创建记录，设置数量、价格、上传图片

### 用户预订流程
1. 用户浏览酒店，选择房型
2. 创建 `room_booking` 记录，`status=1`（待支付）
3. 支付成功后更新 `status=2`（已支付）
4. 入住后更新 `status=3`（已入住）
5. 退房后更新 `status=4`（已完成）

后面的支付入住 流程在本次项目中可能不会完成
